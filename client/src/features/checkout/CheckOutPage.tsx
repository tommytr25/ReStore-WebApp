import { Typography } from "@mui/material";

export default function CheckOutPage() {
    return (
        <Typography variant="h3">
            Only logged in users could see this page!
        </Typography>
    )
}